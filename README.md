# backend_typescript

## Available Scripts

In the project directory, you can run:

### `yarn run compile`

Compile from TS to JS

### `yarn run start`

Execute application

** Need command above is already executing

### `docker-composer up`